(function() {
	
	
	/**
	 * not yet implemented.
	 * @class ListView
	 */
	Class('benignware.view.ListView', (function() {
		
		
		/**
		 * the header element of the list view.
		 * @property headerElem
		 * @return {Element} the header element
		 */
		
		/**
		 * the footer element of the list view.
		 * @property footerElem
		 * @return {Element} the footer element
		 */
		
		/**
		 * renders cells in the list view. 
		 * the default cell renderer function will render instances of benignware.controls.ListItem. 
		 * @property cellRenderer
		 * @return {Function} the cell renderer method
		 */
		
		
		/**
		 * sets up list data
		 * @privileged
		 * @method setData
		 * @param {Array} array an array containing data sets.
		 */
		
		/**
		 * gets item data
		 * @privileged
		 * @method getData
		 */
		
	})());
	
	
})();